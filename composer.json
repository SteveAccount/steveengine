{
  "name": "steveaccount/steveengine",
  "description": "My PHP engine",
  "type": "library",
  "require": {
    "php": "^7.0"
  },
  "autoload": {
    "psr-4": {
      "SteveEngine\\": "/"
    }
  },
  "require": {
    "twig/twig": "^1.6.5",
    "ext-json": "*"
  },
  "scripts": {
    "post-install-cmd": [
      "mkdir -p project-dir/created-directory",
      "touch project-dir/created-directory/created-file.txt"
    ],
    "post-update-cmd": [
      "mkdir -p project-dir/created-directory",
      "touch project-dir/created-directory/created-file.txt"
    ]
  }
}
